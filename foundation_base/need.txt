Need to switch 

The State config widget should be considered a state controller AppState if its redux than AppStore

When I lift out that state its still AppState even with Change Notifier

So its not pure MV* as its mSV* instead





